{
  "name": "kstore-desktop",
  "private": true,
  "version": "1.0.0",
  "description": "Welad Halal POS desktop shell — main window + second-monitor Customer Display (§2)",
  "main": "src/main.js",
  "scripts": {
    "start": "electron .",
    "rebuild": "electron-rebuild -w better-sqlite3",
    "test": "node --test test/offline-store.test.js"
  },
  "dependencies": {
    "better-sqlite3": "^11.10.0"
  },
  "devDependencies": {
    "@electron/rebuild": "^3.7.0",
    "electron": "^33.2.0"
  }
}
